name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
<<<<<<< HEAD
        run npm install
=======
        run: npm install
>>>>>>> any better?

      - name: Deploy site
        run: npm run deploy
